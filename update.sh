#!/bin/bash
# FarmNotes - One-Click Updater for Linux & Chromebook (Crostini)
cd "$(dirname "$0")" || exit

echo "========================================================"
echo "  FarmNotes - อัปเดตเวอร์ชันใหม่ล่าสุด (Chromebook / Linux)"
echo "========================================================"
echo ""
echo "[*] กำลังดึงไฟล์อัปเดตล่าสุดจาก GitHub..."
git pull origin main

if [ $? -eq 0 ]; then
    echo ""
    echo "[OK] อัปเดตเวอร์ชันล่าสุดเรียบร้อยแล้ว! ข้อมูลโน้ตเดิมยังอยู่ครบ 100%"
else
    echo ""
    echo "[!] มีข้อผิดพลาดเกิดขึ้นในการดึงไฟล์ กรุณาตรวจสอบการเชื่อมต่ออินเทอร์เน็ต"
fi
echo "========================================================"
echo ""
